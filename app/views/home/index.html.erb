<% unless current_user %>
  <%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path %>
<% else %>
  <div>
    Hello, <%= current_user.email %>
  </div>
  <div>
     <%= react_component('CoursesList', prerender: false) %>
  </div>
<% end %>

